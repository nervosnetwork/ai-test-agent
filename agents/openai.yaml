interface:
  display_name: "AI Test Agent"
  short_description: "Review PR intent, test trees and assertion evidence"
  default_prompt: "Use $ai-test-agent for one scope and gate: explain the PR, build Spec/test tree and B design review, stop for confirmation, then implement direct TEST-MAP tests and have B review evidence before focused execution."
