interface:
  display_name: "AI Test Agent"
  short_description: "Review test intent before mapped automation"
  default_prompt: "Use $ai-test-agent for one interface and one review gate at a time. Propose concise cases and stop for confirmation; then implement only confirmed IDs as direct TEST-MAP tests with focused verification."
