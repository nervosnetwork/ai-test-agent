# {{SUITE_LABEL}} Review Documents

Create one Markdown file per coherent interface or behavior from root `templates/test-review.md`.

Present every new, deleted, or materially changed row for explicit human confirmation before generating or changing mapped automation. Use the review case ID directly in code comments as `TEST-MAP: <CASE-ID>`.

Read `review-feedback.md` before analysis when it exists. On corrective human feedback, create or append that file with one line using the root `AGENTS.md` format; do not record approval-only responses.
