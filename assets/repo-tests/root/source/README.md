# Product Source Workspace

Follow `../AGENTS.md`: reuse the matching checkout under `{{PROJECT_SLUG}}/`, or clone the declared repository there only when absent. Product source under this directory is long-lived local working data and must not be committed to the generated test project.
